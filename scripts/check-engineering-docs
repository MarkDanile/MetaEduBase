#!/usr/bin/env python3
from __future__ import annotations

import runpy
from pathlib import Path


TOOL = (
    Path(__file__).resolve().parent
    / "engineering"
    / "check_engineering_docs.py"
)


if __name__ == "__main__":
    runpy.run_path(str(TOOL), run_name="__main__")
