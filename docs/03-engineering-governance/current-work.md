# 当前开发工作台

本文件是所有 AI IDE、插件和人工协作的当前任务入口。开始任何开发任务前，先阅读本文件，再按任务卡片中的链接渐进式读取相关 spec、plan、技术债或架构约束。

不同任务类型的开工条件、必读文档和完成标准见 `docs/03-engineering-governance/task-modes.md`。

## 使用规则

- 本文件只保留当前任务、近期候选和少量最近完成任务；任何修改本文件或任务状态前，必须先读 `docs/03-engineering-governance/01-rules/workbench.md`。
- 开发前确认本次任务卡片，并按卡片链接渐进式读取 spec、plan、技术债或架构约束。
- 涉及跨文件开发、计划接力、状态交接或后续继续开发时，必须登记或更新任务卡片。
- 代码、验证或 Git 阶段变化后，必须同步任务状态、当前进展、下一步和验证结果。
- 提交、PR、合并或声明完成前，运行 `scripts/check-engineering-docs` 并执行 `docs/03-engineering-governance/01-rules/quality-gates.md#完成门禁`；门禁主实现位于 `scripts/engineering/check_engineering_docs.py`。

## 当前进行中

### TASK-R1-S6-I3: S6-F1..F14 故障矩阵 + TD-106 方案 A bounded integration（scope 已收敛；PR-D ledger export/replay+runbook、PR-E release drill 拆分独立后续 PR）

状态：🟢 已 squash merge 入 main（2026-08-26，**mergeCommit `68fafd81de5b0925be44e935c532cdce898e2935`**，source head `cca892b54b39836cc5e98a5e84e3028ba1ddea04`，FINAL_IMPL_HEAD `1a61ba6f`，评分 94 Original 已落入 Score Log；评审对象 main@`ddcb87ca`..`1a61ba6f`）；scope = F1-F14 故障矩阵 + TD-106 方案 A bounded integration；TD-106 已完成并关闭
最新交接（2026-08-26）：**scope 收敛 + root 三面复审 + Ready + 评分 94 + squash merge 入 main + closeout commit（已完成）**。**集成事实链**：`PR #589` (R1-S6-I3-A schema/test alignment bounded repair, squash 入 #586 mergeCommit `f6062466`, 评分 92 Original) + `PR #590` (R1-S6-I3-C S6-F1..F14 故障矩阵 + TD-106 方案 A 实现, squash 入 #586 mergeCommit `ee53b172`, 评分 94 Original; source head `cd5a437c`, FINAL_IMPL_HEAD `d3a12549`) + `PR #592` (TD-106 方案 A 实现, squash 入 #590 mergeCommit `e345c429`, 评分 92 Original) → `PR #586` (squash merge 入 main `68fafd81`, source head `cca892b5`, FINAL_IMPL_HEAD `1a61ba6f`, 评分 94 Original) → closeout commit（main closeout PR 已 squash merge 入 main）。scope 收敛（commit `9eef73cd`）撤回 PR-D/E scaffold（`s6i3_ledger_export.py` / `s6i3_restore_replay.py` / `s6i3_release_drill.py` / `restore-before-open.md` / `test_s6i3_fault_matrix_restore_replay.py` 18 项 PR-D/E 测试）——**本 root = F1-F14 故障矩阵 + TD-106 方案 A bounded integration；PR-D/E 拆分独立后续 PR**。**三面复审**（#586 root, head=`1a61ba6f`，评审对象 main@`ddcb87ca`..`1a61ba6f`）：面 A 数据/状态/scope + 面 B 并发/锁序/恢复 + 面 C 测试/mutation/ops/docs → 三面 P0=0/P1=0/P2=0/P3=3（不修复）；验证基线——fresh PG head=043 + F1-F14+TD-106 专项 37 passed/1 skipped（F10）+ TD-106 专项 20/20 + composition 全量 763 passed/1 skipped（撤回 18 项 PR-D/E 测试）+ migration roundtrip 10/10 + ruff/mypy baseline(0 reg)/git diff --check/engineering-docs --full 全绿。**TD-106 已完成并关闭**——bounded integration 已并入 main；main 现包含完整实现；migration 043/schema/enum/CHECK/CI/门禁脚本/KNOWN_ISSUES 零改动。**整体仍未完成（不属于本 root 范围）**：F10 仍 skip（`test_f10_contract_conflict_not_implemented`，TD-105 承接——前置 TD-106 现已完成，可启动 F10 实现 PR 解除 skip）；#590 mutation 9/12 真红 + M-F3/M-F5/M-F8 停止条件登记保持；PR-D/PR-E/C1/S5 production wiring/registry capability 翻转（external/runtime 保持 `erase_available=False`）均未启动；TD-104（PR-A schema/test alignment 残项）+ REQ-047（R1-S6 implementation conformance 联合闭环）保持后续承接；F-matrix 7/12 mutation NOT-RED（M-F2/F3/F4/F5/F6/F8/F12）留作独立测试 contract 增强 PR。
类型：REQ-041/047 R1-S6-I3（R1-S6 最后一片实现；S6-I1/I2/I3 已合并并 closeout）
领域：scheduler / retention / purge-recovery / fault-matrix（PR-D ledger export/replay、PR-E release drill 已拆分独立后续 PR，本 root 不含）
当前执行模式：实现（contract-first S6-5/S6-7/S6-8 已随 PR #581 冻结并入 main；本 PR 已实现 S6-I3 业务代码与测试 + 评分 94 Original）
最近接手工具：Claude Code
分支：`feature/req041-047-r1-s6-i3-fault-matrix-restore-replay` 已删除（`gh pr merge 586 --squash --delete-branch`）

需求来源：
- Spec: [R1 Retention/Purge/恢复专项契约 §10/§11](../02-delivery-plans/01-specs/2026-07-27-req-041-047-r1-retention-purge-recovery.md)
- Plan: [R1 分 Slice 实施计划 §R1-S6-5/§R1-S6-7/§R1-S6-8/§R1-S6-9/§R1-S6-10/§R1-S6-15](../02-delivery-plans/02-plans/2026-07-27-req-041-047-r1-retention-purge-recovery-plan.md)（S6 契约冻结经 PR #581 并入 main `01524667`；S6-I1 经 PR #582 squash `f5072ec6`；S6-I2 经 PR #584 merge `ad7ac3e5` + closeout PR #585 merge `96ddc014`；F10 契约纠偏 + TD-106 决策门禁 经 PR #591 squash `738be6f9` + closeout PR #593 squash `ddcb87ca`；TD-106 方案 A 实现经 PR #592 `e345c429` → #590 `ee53b172` → #586 `68fafd81` 入 main；F10 真实 PG 判别 + mutation kill 经 PR #596 squash `c0ec008d` 入 main，main 现 `c0ec008d`）

下一步：root 已合 main，下一步**启动后续 PR**（按建议执行顺序）：(1) **PR-D**（ledger export executor + restore-before-open runbook，plan §S6-8/§S6-12/§S6-13）；(2) **PR-E**（release drill 五阶段 canary，plan §S6-7）；(3) **F-matrix 7/12 + F10 M6 test contract 增强 PR**（独立测试 contract 增强）；(4) **TD-104**（PR-A schema/test alignment 残项）。**F10 已完成（PR #596 已合 main `c0ec008d`）**：TD-105 由 PR #596 承接并关闭；main closeout 已记录 F10 routing 四环 + TD-106 不变式 + 7/8 mutation 真红 + M6 NOT-RED 登记。**本 root 不含、本轮不启动**（已登记为后续 PR）：PR-D / PR-E / C1 Durable Core 总验收 / S5 production wiring / registry capability 翻转（external/runtime 保持 `erase_available=False`）/ 六 erase 入口生产可达 / F-matrix 7/12 + F10 M6 test contract 增强 / TD-104 均未启动。

验证状态：scope 收敛后本地验证（PR #586 合 main 前）——fresh PG head=043 + F1-F14+TD-106 专项 37 passed/1 skipped（F10）+ TD-106 专项 20/20 + composition 全量 763 passed/1 skipped（撤回 18 项 PR-D/E 测试）+ migration roundtrip 10/10 + ruff/mypy baseline(0 reg)/git diff --check/engineering-docs --full 全绿；PR #586 三路 required checks 全 SUCCESS（Backend ~14min `Backend` at 2026-08-26T07:30:48Z + Engineering docs 17s `Engineering docs` at 2026-08-26T07:16:23Z + Frontend 2m14s `Frontend` at 2026-08-26T07:18:36Z）。**严格停止条件**：发现 P0/P1、需新 schema/migration、需修改 S5 状态机/锁序/写者矩阵、需翻转任一 registry capability、需 production scheduler wiring 或六 erase 入口可达、需真实生产 canary/backup/restore drill 才能宣称完成、replay 需调用 external/runtime adapter、无法证明旧 ledger owner_version/digest、发现与 S6-5/S6-7/S6-8 冻结语义冲突——立即停止并报告，不自行架构裁决。**禁止修改**：Metrics、Score Log、migration 043、门禁脚本、KNOWN_ISSUES、CI 配置或阈值。**未启动**：F10 实现 PR（TD-105 承接）/ PR-D（ledger export/replay executor + runbook）/ PR-E（release drill）/ C1 Durable Core 总验收 / S5 production wiring / registry capability 翻转（external/runtime 保持 `erase_available=False`）/ 六 erase 入口生产可达 / F-matrix 7/12 mutation NOT-RED 增强 PR / TD-104。

交接备注：R1-S6-I1/I2 已合并 main `96ddc014`；R1-S6-I3-A 已 squash 入 #586（PR #589 `f6062466` 评分 92）；R1-S6-I3-C 已 squash 入 #586（PR #590 mergeCommit `ee53b172` 评分 94，source head `cd5a437c`、FINAL_IMPL_HEAD `d3a12549`）；TD-106 方案 A 经 #592→#590→#586→main 完整闭合（mergeCommit `68fafd81` 评分 94 Original，source head `cca892b5`、FINAL_IMPL_HEAD `1a61ba6f`，评审对象 main@`ddcb87ca`..`1a61ba6f`）——**TD-106 已完成并关闭**；main closeout PR 已 squash merge 入 main（pure-docs）。**整体 S6-I3 仍未完成**：F10 仍 skip（TD-105 承接）+ PR-D/PR-E/C1/S5 wiring/capability flip 均未启动；TD-104/REQ-047 与既有 follow-up 不关闭；M-F3/M-F5/M-F8 mutation 停止条件登记保持；F-matrix 7/12 mutation NOT-RED（M-F2/F3/F4/F5/F6/F8/F12）留作独立测试 contract 增强 PR；restore_replay_executor 的 PR-D 骨架已随 scope 收敛撤回；S6I2_PENDING_WRITERS 中 restore_replay_executor 仍仅 pending（字符串登记、无 impl），待独立 PR-D 重建后转 registered，不接生产 wiring；TD-097/098/099/100/101/102/103 保持历史和 follow-up 编号，不因本 PR 自动覆盖或关闭；REQ-047 保持后续联合验收归属。后续收口顺序（已完成）：#590→#586（mergeCommit `ee53b172`）→ #586 root 三面复审/Ready/评分 → #586 squash→main（mergeCommit `68fafd81`）→ main closeout PR（squash merge 入 main）— **全流程闭合**。
后续接力：PR #596 已 squash merge 入 main `c0ec008d`（source head `f5cb0b34` / implementation HEAD `7cc732ba`，评分 97 Original）。**F10 已完成**（TD-105 已关闭）——F10 routing 四环 + TD-106 per-ref receipt + source 清除不变式 + 7/8 mutation 真红 + M6 NOT-RED（如实登记为 test contract 增强 follow-up）；后续接力：(1) PR-D / (2) PR-E / (3) F-matrix 7/12 + F10 M6 test contract 增强 PR / (4) TD-104（PR-A schema/test alignment）/ (5) C1 Durable Core 总验收 / (6) S5 production wiring / (7) registry capability 翻转 / (8) 六 erase 入口生产可达。

### TASK-R1-S6-I3-D: 独立 ledger export/archive + restore replay executor + restore-before-open runbook（**D1a + D1b 均已 squash merge 入 main + main closeout；D2 = PR #602 OPEN/Draft 实现中；PR-D 整体未启动** — 不宣称 runbook/PR-D 已交付；D1a 已 squash merge 入 main，D1b 已 squash merge 入 main，D2 = PR #602 Draft，PR-D 仍未启动）

状态：🟡 进行中（2026-08-28，**D1a bounded + 三轮 P1 治理闭环已 squash merge 入 main** —— PR #598 source head `ca9f4404884c52f5c7fd9851d8132f6d696c6e13` + FINAL_IMPL_HEAD `8a836733f6528f959825d14285a2f44871ef7606` + mergeCommit `5868831ee0a8142bb1f41766ab590b54d21bd9ea`，评分 97 Original；**D1b 专用 MinIO ledger archive sink + 不可变 commit-graph 发布协议（两阶段 API 拆分）已 squash merge 入 main** —— PR #600 source head `467e8d24a74f0c68ef2325340f7f86e2cca4397c` + FINAL_IMPL_HEAD `061f4a66` + 评分 commit `1293ea5194ccef9086188acf5677c51a4bf1d932`（correction commit `467e8d24`，in-place 行修订事实更正） + mergeCommit `01c84f7c44de693642ac71150f9bf377e8685539`，评分 94 Original；三面 P1 已修复：删除 `published_at_unix` 伪造时间 / 拆两阶段 API / 新增 opt-in 真实 MinIO acceptance —— **真实 MinIO acceptance 已执行 6/6 PASS（per-run UUID bucket + try/finally cleanup + metaedu-resources 未触碰）**；用户裁决 5 项已**冻结**：runtime per-binding proof = c / D1b = 专用 MinIO archive bucket / D2 = A advisory lock / D1a-D1b-D2 三独立 PR / 顺序 D1a → D1b → D2；**D2 = PR #602 OPEN/Draft（实现中，writer 已 registered）；D1b production wiring / D2 production wiring / PR-D / PR-E / C1 / S5 production wiring / capability flip / 六 erase 入口生产可达仍未启动**）

最新交接（2026-09-01，**D2 Round-8 有界重构返修**）：**唯一当前事实**——D1a 已入 main（PR #598 mergeCommit `5868831e`，评分 97 Original）；D1b 已入 main（PR #600 mergeCommit `01c84f7c`，评分 94 Original）；D2 = PR #602 OPEN/Draft（分支 `feature/req041-047-r1-s6-i3-d-d2-restore-replay`），writer `restore_replay_executor` 已 **registered=FENCE_M**（`app.composition.restore_replay.replay_archive_segment_for_tenant`，登记于 `s6i2_orphan_inspection.py`）；**未启动**：D2 production wiring / PR-D / PR-E / C1 / S5 production wiring / capability flip / 六 erase 入口生产可达。**Round-8 保持 OPEN/Draft（不 Ready / 不评分 / 不合并 / 不 closeout）**，3 项 P1 + 2 项低成本（详见 PR #602 body 与 git log）：(1) pass B 报告提交边界——事务内 verdict 仅 provisional，仅 `async with session.begin()` 成功退出后发布 committed verdict；任一 owner 失败 rollback 时 error report 不保留任何已回滚 success verdict（改标 `ACTION_ROLLED_BACK`），新增真实双 owner PG 判别（participant failure + TOCTOU 后序失败）；(2) mutation JUnit 分类器——crash / setup / fixture / teardown error 优先于 KILLED，仅 saw_invariant 且无 crash/setup 才计 KILLED，新增 mixed assertion+error / assertion+crash / 跨 testcase mixed 三类自测，全部 mutation 用修正后分类器重跑；(3) 本任务卡前部重写——删除「D2 未启动 / restore writer pending / 尚未启动 archive sink / restore replay executor」等当前态冲突。低成本：external-ref binder 先严格校验 owner_key 类型再匹配（malformed owner + 合法匹配共存反例）；mutation JUnit 临时目录改 TemporaryDirectory 自动清理。**Round-8 验证基线（本地）**：D2 专项 108（90 restore_replay + 18 classifier 自测）；composition 全量 992 passed / 6 skipped；mutation 用修正后分类器重跑 **21/21** KILLED（byte-identical restore）；ruff clean；mypy baseline 243 historical / 0 回归 + restore_replay.py targeted mypy no issues；git diff --check clean；engineering-docs `--full` 全绿（32 known issues allowlisted）；td-032 已按 Round-8 实际行数 1876 / 2559 / 253 / 671 更新登记。**historical（已被 supersede，非当前态）**：早期 Round 的「尚未启动 archive sink / restore replay executor」「writer 仅 pending」表述对应 D1a-only 阶段，已被 D1a/D1b 合 main + D2 落地 PR #602 supersede；Plan §S6-8/§S6-12/§S6-13/§S6-14 冻结的 ledger export 与 restore replay 路由表 + 判定方式为唯一事实源；旧 PR-D scaffold（`s6i3_ledger_export.py` / `s6i3_restore_replay.py` / `restore-before-open.md` 等）已随 #586 scope 收敛撤回，**禁止 cherry-pick / 恢复 / 复制旧 `c07c031c` scaffold**。
分支：`feature/req041-047-r1-s6-i3-d-d2-restore-replay`（D2 实现分支，PR #602 OPEN/Draft）
当前执行模式：D2 Round-8 有界重构返修（普通新 commit，不 amend/rebase/force-push，保持 OPEN/Draft；仅 metaedu_test），未启动 D1b production wiring / D2 production wiring / PR-D / PR-E / C1 / S5 production wiring / capability flip / 六 erase 入口生产可达
最近接手工具：Claude Code
类型：REQ-041/047 R1-S6-I3-D（PR-D：独立 ledger 连续导出/归档 + restore replay 执行器 + restore-before-open runbook）
领域：scheduler / retention / purge-recovery / restore-replay（M 类维护路径）
需求来源：
- Spec: [R1 Retention/Purge/恢复专项契约 §3 / §10 / §11](../02-delivery-plans/01-specs/2026-07-27-req-041-047-r1-retention-purge-recovery.md)（§3「从**独立保存**的 erasure operation/receipt 账本重放」字面要求 + §10 发布迁移流程 + §11 R1-AC12 验收标准）
- Plan: [R1 分 Slice 实施计划 §R1-S6-8 / §R1-S6-12 / §R1-S6-13 / §R1-S6-14 / §R1-S6-15](../02-delivery-plans/02-plans/2026-07-27-req-041-047-r1-retention-purge-recovery-plan.md)（§S6-8 备份恢复门禁 + §S6-12 replay 状态路由表 + §S6-13 replay 判定方式 + §S6-14 后续拆分）
- 需求: [REQ-047 Agent Run/产物/证据与人工确认中心](../01-product-planning/05-requirements/REQ-047-agent-run-artifact-approval-center.md)（R1-S6 implementation conformance 联合闭环承接）
- 技术债: `S6I2_PENDING_WRITERS` 登记（`restore_replay_executor` 已 **registered=FENCE_M**；historical：早期曾仅 pending）

下一步（**D1a 已 squash merge 入 main**——`5868831e`；**D1b 已 squash merge 入 main**——`01c84f7c`；D2 / PR-D / PR-E / C1 / S5 production wiring / capability flip / 六 erase 入口生产可达仍未启动）：
- **Phase 0（已落）**：本轮输入 baseline=`4cf3ab36`；修正 ack 约束三层职责分离 / M 类互斥结论不强制新 flag / runtime per-binding proof 不可重算登记为未决 P1 / 区分 CLI ≠ sink / 三方案互斥决策矩阵并列评估 / D1a-D1b-D2 边界修正；用户裁决 5 项（Runtime proof=c / D1b=专用 MinIO archive bucket / D2=A advisory lock / D1a-D1b-D2 三独立 PR / 顺序 D1a-D1b-D2）
- **Phase 1 D1a only（已完成 + 已 squash merge 入 main `5868831e`）**：
  - 实施 ledger snapshot codec（只读 codec + bounded snapshot/segment exporter + decode/validate + reconstruct_owner_facts）——3 文件（`s6i3_ledger_snapshot.py` + `test_s6i3_ledger_snapshot.py` + `s6i3_d_snapshot_mutation_kill.py`）
  - **D1a 第一轮 P1 bounded/严格化（2026-08-27 用户裁决，已落）**：
    1. **真正 bounded**：`MAX_RECORDS_PER_KIND=10000` + `ORDER BY id LIMIT max+1`；超限抛 `SEGMENT_LIMIT_EXCEEDED`，**不**截断后冒充完整 snapshot；不持久推进 watermark（仍属 D1a）
    2. **删除误导 runtime_aggregate**：从 envelope + decoder 移除 `runtime_acked_count / runtime_invalid_count / runtime_open_count`；checkpoint records 承载 owner 聚合事实；envelope 仅保留 `runtime_per_binding_proof_available=false`；decoder 强制严格 false（缺失或 True → `RUNTIME_PROOF_FLAG_MISSING` / `RUNTIME_PROOF_FLAG_TRUE`）
    3. **六元组不得覆盖**：`reconstruct_owner_facts` 返回以 `(operation_id, owner_key)` 为键的映射；新增 `test_d1a_two_operations_same_owner_key_preserved` 真实 PG 测试证明同 owner_key 两 operation 均保留；`purge_operation_id` 缺失 / operation 缺失 / 六元组字段缺失均在 decode 阶段 fail closed（`CHECKPOINT_WITHOUT_OPERATION` / 隐式 KeyError）
    4. **强制事务属性**：`export` 入口 `_assert_transaction_attrs(conn)` 核验 `SHOW transaction_isolation == 'repeatable read'` + `SHOW transaction_read_only == 'on'`；读已提交 / RR read-write 立即拒绝（`TX_ISOLATION_NOT_REPEATABLE_READ` / `TX_NOT_READ_ONLY`）；保留无事务拒绝（`EXPORT_REQUIRES_TRANSACTION`）
    5. **Decoder 严格性**：operation.state / checkpoint.state 白名单（migration 034 CHECK 闭集）；跨层值 → `CROSS_LAYER_STATE_MIX`（`reason=state_out_of_whitelist`）；未知 kind → `UNKNOWN_KIND`；非 object record → `RECORD_NOT_OBJECT`；非 mapping fields → `RECORD_FIELDS_NOT_MAPPING`；乱序 → `RECORDS_NOT_SORTED`；checkpoint.state/ack_digest 基本关系（migration 034 ck_agent_purge_owner_ack）→ `ACK_INVARIANT_VIOLATED`
  - **D1a 第二轮 P1 逻辑身份 / 六元组完整性 / tenant 绑定 / strict decoder（2026-08-27 用户裁决，本轮已落）**：
    1. **逻辑身份绑定**（decoder 阶段）：每条 record `stable_identity` 严格 == `f"{record_kind}:{fields.id}"`；缺 id / 不匹配 → `STABLE_IDENTITY_BINDING_MISMATCH`；operation.fields.id 重复 → `DUPLICATE_OPERATION_ID`；checkpoint `(purge_operation_id, owner_key)` 重复 → `DUPLICATE_CHECKPOINT_OWNER_KEY`
    2. **OWNER_SIX_TUPLE_INCOMPLETE 在 decode 阶段实现**：checkpoint 五字段缺失（`owner_key` / `purge_operation_id` / `owner_version` / `capability_digest` / `state`，pytest 参数化 5 项）+ operation.purge_revision 缺失均在 decode 阶段即抛，不等待 `reconstruct`；`reconstruct_owner_facts` 同时新增防御性兜底——绝不以 sentinel / 默认值静默构造 owner fact（仍以具名 `OWNER_SIX_TUPLE_INCOMPLETE` / `CHECKPOINT_WITHOUT_OPERATION` 抛出）
    3. **Tenant 绑定**：decode 签名新增 `*, expected_tenant_id: uuid.UUID`；declared tenant_id 与 expected 不一致 → `TENANT_BINDING_MISMATCH`；tenant_id 必须是规范 UUID（`uuid.UUID()` 解析）→ 否则 `TENANT_ID_NOT_UUID`；新增空 artifact tenant 替换反例（不依赖 records 非空）；D1b 后续边界登记——archive object identity / content digest 仍须绑定 tenant（本轮不启动 MinIO）
    4. **Strict decoder 补齐**：manifest 顶层 keys 严格 == `RECORD_KINDS`（额外 → `MANIFEST_KIND_UNKNOWN`）；新增 manifest-only unknown kind 反例；ACK 64-hex 严格小写 hex（应用层门禁，与 migration 034 `length=64` 区分——**不得**误称为"migration 034 要求 64-hex"）
  - **D1a 第三轮 P1 严格基础类型 / canonical tenant / 六元组类型闭合 / consumer bounded（2026-08-27 用户裁决，本轮已落）**：
    1. **严格基础类型与 canonical tenant**：`schema_version` 严格 int（排除 bool，Python `isinstance(True, int)==True` 必须显式 `not isinstance(sv, bool)`）；`manifest[*].count` 严格 int（排除 bool）；`tenant_id` 必须满足 `str(uuid.UUID(tid)) == tid`（**canonical form**：lowercase + 8-4-4-4-12 连字符），大写 / 去连字符 / 含其他字符 → `TENANT_ID_NOT_CANONICAL_UUID`；`manifest[*].content_digest` 严格小写 64-hex（`re.compile(r"^[0-9a-f]{64}$")`），**不**只校验长度
    2. **六元组类型闭合 + 异常归一化**：decode 阶段新增 `_assert_field_types` 严格按真实 DB 列语义校验（`operation.id` / `checkpoint.id` / `checkpoint.purge_operation_id` canonical UUID；`operation.purge_revision` / `checkpoint.owner_version` 严格 int；`checkpoint.owner_key` string；`checkpoint.capability_digest` 64-hex 小写；`checkpoint.state` string；非法 → `LedgerSnapshotError`，**不得**让 `ValueError`/`TypeError`/`KeyError`/`AttributeError` 漏出）；`reconstruct_owner_facts` 外层 try/except 兜底——即使调用方直接构造 Manifest 绕过 decoder，所有原始异常归一化为 `LedgerSnapshotError("RECONSTRUCT_INTERNAL_TYPE_ERROR", ...)`，绝不静默转换或覆盖
    3. **Bounded 消费端闭合**：decoder 在 `decode_ledger_segment` 内对每个 record kind 强制 `MAX_RECORDS_PER_KIND=10_000` 上限（与 producer 端 `LIMIT max+1` 形成 defense-in-depth）；10001 条合法摘要 artifact 在 decode 阶段即抛 `SEGMENT_LIMIT_EXCEEDED`，**不**新增 byte-size / watermark / sink / 任何新契约，本轮只闭合已有 per-kind 上限
    4. **纯内存 decoder 负例**：新增 15 项 `_memory` 测试（不依赖 PG；纯 `_build_envelope_one_record` + `_envelope_with_digest` + `_envelope_to_bytes` 构造摘要合法的异常 artifact）；覆盖 schema_version=True / count=False / 非 canonical 大写 tenant / 非 canonical 去连字符 tenant / 非 hex manifest digest / 大写 hex manifest digest / owner_version bool / purge_revision bool / owner_key int / capability_digest int / checkpoint id 非 canonical / operation id 非 canonical / operation purge_revision string / 直接构造 Manifest 走 reconstruct 兜底 / 10001 条合法摘要 artifact 触发 `SEGMENT_LIMIT_EXCEEDED`——避免 `CONTENT_DIGEST_MISMATCH` 抢先掩盖目标断言
  - **D1a 严格限定**：不写文件 / 不接 sink / 不持久推进 watermark / 不做 DB mutation / 不调 adapter；D1a 是 bounded read-only artifact，**不**称 continuous archive / **不**称 replay executor
  - **真实 PG 测试覆盖**：42 项 D1a 专项（15 baseline + 11 第一轮 P1 + 2 mutation 配套 + 9 第二轮 P1 严格化 + 1 reconstruct 防御性兜底 + 4 mutation 第二轮 P1 配套）——bounded limit / RR-RR-RO / RR-RC 拒绝 / RR-RR-RW 拒绝 / 两 op 同 owner_key 保留 / operation.state 越界 / checkpoint.state 越界 / ACK 64-hex 缺失 / records 乱序 / unknown kind / runtime proof 缺失 / runtime proof True / count 篡改 / cross-tenant 篡改 / ref_value 不泄露（precondition: external_ref 非空）/ stable_identity id 不匹配 / duplicate operation id / duplicate checkpoint owner key / 5 字段参数化 OWNER_SIX_TUPLE_INCOMPLETE / purge_revision 缺失 / tenant binding mismatch empty artifact / tenant not uuid / manifest unknown kind / ack not hex / reconstruct owner fact 缺失兜底 / 其余原 15 项
  - **纯内存 decoder 负例**：15 项 `_memory` 测试（schema_version bool / count bool / tenant uppercase / tenant no-hyphens / manifest digest uppercase hex / manifest digest non-hex / owner_version bool / purge_revision bool / owner_key int / capability_digest int / checkpoint id non-canonical UUID / operation id non-canonical UUID / operation purge_revision string / reconstruct Manifest 兜底 / consumer segment limit exceeded 10001）——避免 `CONTENT_DIGEST_MISMATCH` 抢先掩盖目标断言；**42 真实 PG 测试全部保留未删**
  - **Mutation kill**：**20/20 真红**——M1 tenant WHERE bypass / M2 ref_value 泄露 / M3 digest bypass / M4 count bypass / M5 schema_version bypass / M6 kind-table bypass / M7 stable sort corruption（producer 反向输出 corruption，**非**"删除 sorted 必然可观察"——原 mutation 形如 `tuple(operation)` 不可观察因 SELECT 已 ORDER BY id；保留真红，PR body / current-work 不再称证明 sorted() 调用不可删除） / M8 duplicate identity bypass / M9 cross-tenant bypass / M10 runtime proof false-positive / M11 identity binding bypass / M12 duplicate owner-fact key bypass / M13 owner tuple completeness bypass / M14 expected tenant binding bypass / **M15 schema_version strict_int bypass**（接受 bool True） / **M16 tenant canonical bypass**（仅检查可解析，不强制 canonical） / **M17 manifest count strict_int bypass**（接受 bool False） / **M18 manifest content_digest 64-hex bypass**（仅检查长度 64，不要求小写 hex） / **M19 field type bypass**（`_assert_field_types` 直接 return，绕过 owner_version / purge_revision / owner_key / capability_digest / canonical UUID 校验） / **M20 consumer MAX_RECORDS_PER_KIND bypass**（`_assert_max_records_per_kind` 直接 return）
  - **本地验证**：D1a 专项 **57/57 pass**（42 真实 PG + 15 纯内存）；composition 全量 **828 passed**（813 → 828，+15 D1a 第三轮纯内存专项）；mutation kill **20/20 KILLED**；ruff clean / mypy 官方 baseline gate `cd packages/server-python && uv run --frozen --extra dev python scripts/check_mypy_baseline.py` → `passed: 243 historical errors across 76 keys; 0 regressions`（D1a 文件 **不**在 main 上；本 PR 0 回归）/ 包目录目标文件 `cd packages/server-python && uv run mypy app/composition/s6i3_ledger_snapshot.py` → `Success: no issues found in 1 source file` / git diff --check clean；engineering-docs `--full` 全绿（32 known issues allowlisted，td-032 已按修复后实际行数 1724 / 1913 / 397 行更新）
- **后续接力（按用户裁决顺序 D1a → D1b → D2）**：
  1. **D1b 已 squash merge 入 main `01c84f7c`**（2026-08-28，OPEN/Ready/MERGEABLE；head=`feature/req041-047-r1-s6-i3-d-d1b-minio-archive`；source head `467e8d24a74f0c68ef2325340f7f86e2cca4397c` + FINAL_IMPL_HEAD `061f4a66` + 评分 commit `1293ea5194ccef9086188acf5677c51a4bf1d932` + correction commit `467e8d24` + mergeCommit `01c84f7c44de693642ac71150f9bf377e8685539`；评分 94 Original；含**裁决 1 + 裁决 2 + 裁决 3**：A-1 删除 `CommitMarker.published_at_unix` 伪造时间字段（marker 字节稳定由 segment_sha256 / export_id / generation / parent_export_id / per-kind 共同保证，不承载 wall-clock）+ B-1 拆两阶段 API（`export_ledger_segment_for_archive(session, *, tenant_id)` RR+RO 事务内仅 D1a export + decode 双校验零 sink I/O + `publish_ledger_segment(*, sink, tenant_id, segment_bytes, manifest, parent_export_id=None, sleeper=None)` 严格不接收 AsyncSession 仅 sink I/O 零 DB I/O；`archive_ledger_segment` 删除，caller 显式编排两阶段）+ A-5 per-kind generation 修订（写入顶层 publication generation 而非 DB source cursor，同 marker 内 per-kind 共享顶层值，marker 间单调推进；新增 `test_d1b_per_kind_generation_advances_with_marker_generation` 真实 PG 双代证据 first gen=1 / second gen=2）+ 11/11 behavioral mutation KILLED（subprocess pytest 真实 red→green，每个 mutation byte-level 修改 + 绑定 pytest invariant test + mutation 存在 exit=1 + 恢复后 exit=0 + SHA-256 byte-identical `1d1e3bfd5509f0e3bc26139232d083bb1de701b792e40ce21acaef4e8860f3ab` 一致；M2/M4 重定义为独立故障：M2 = segment 不存在时仍 PUT marker / M4 = 吞 segment PUT 异常 + 继续 marker PUT + 新增 M10 spy decode）+ C-1 真实 MinIO opt-in acceptance 6/6 PASS（per-run UUID bucket `metaedu-d1b-acceptance-{uuid[:12]}-{pid}-{ts_low16}` + try/finally cleanup + 三层防御 `bucket != "metaedu-resources"` / `bucket != settings.minio_bucket` / `bucket not in FORBIDDEN_BUCKETS`；测试后 dev MinIO `total buckets: 0`，metaedu-resources 未被创建/触碰；累计 47/47 D1b composition（含 per-kind generation 双代 1 + M2/M4/M10 mutation red invariants 3）+ 11/11 behavioral mutation KILLED + 6/6 opt-in MinIO + ruff clean + mypy baseline 0 回归 + engineering-docs --full 全绿；**三套独立验证口径禁止合并**）；**D1b 实现本体完成**（专用 MinIO archive sink + 不可变 commit-graph 发布协议 + 两阶段 API 拆分 + per-kind generation sink 端语义），**D1b production wiring**（scheduler 接入 / capability flip / 六 erase 入口生产可达）仍未启动
  2. **D2**（replay executor + M 类互斥 = A 方案 + restore-before-open 编排 + restore 端 DB mutation）——**用户裁决 5 项已 supersede（fact-audit §17.5，2026-08-27）**：(1) Runtime per-binding proof = **c**（archived completed runtime 缺 per-binding proof 时返回具名 `RUNTIME_BINDING_EVIDENCE_UNPROVABLE`；零 DB 写、不修改 terminal operation、不伪造 blocked/acked、不写假 receipt；restore-before-open 保持关闭，转 runbook 人工处置）(2) M 类互斥 = **A**（global `pg_advisory_xact_lock_shared` 给 retention/audit；replay 事务取 `pg_advisory_xact_lock` exclusive；新锁必须在 Run/Conversation/owner/collection 锁之前取得；同一稳定 namespace/scope；**须写作 S6-4 锁序登记修订**）(3) D1a+D1b+D2 = **三独立 PR**（D1a / D1b / D2 各自独立 PR，不合并）(4) 顺序 = **D1a → D1b → D2**（D1a 已合 main `5868831e`；D1b 已合 main `01c84f7c`；D2 在本任务卡对应分支 `feature/req041-047-r1-s6-i3-d-d2-restore-replay` 内启动）(5) D1b = **专用 MinIO archive bucket**（`metaedu-ledger-archive` ≠ `metaedu-resources`，已随 PR #600 落地）——**Round-1 P1 返修已落地（commit `6f3b2a3b`）**：6 项冻结边界（4 owner → 4 participant 公共入口 / pass A 六元组重验 / 删自造 ACK / external-runtime 分离 / 两遍执行 / gate 复用 `build_scan_providers`）+ 8 项新判别测试；**Round-4/Round-5 P0/P1 修复已落地（2026-08-31）**：pass A cp_state 单向终态转换特例（archive=erasing/pending + LIVE=acked → NO_REPEAT，**禁止**无证据 drift 走 NO_REPEAT）+ pass B 全字段 TOCTOU reverify（11 字段：operation 5 字段 + checkpoint 6 元组）+ external verify-only 按 `validated.conversation_id` + `owner_key` 精确绑定 archive external_ref + final scan（多 record / 缺 / 错绑 fail closed，**禁止**回退任意 LIVE row）+ archive facts 严格解析（`_require_field` 具名 `ARCHIVE_FACTS_*_MISSING` / `*_TYPE_INVALID`，删除 `get(...) or 0` / `str()` / `int()` 默认化；pass A ValueError/TypeError/KeyError/AttributeError 统一转 fail-closed report，**不**回填 LIVE 值）；mutation kill 强化为真红证据版——M-D2-14/15/17 由不完整 SyntaxError mutant 重写为完整可运行 Python（catch-and-continue partial commit / `_require_field` 缺失字段默认 0 fallback / verified-without-receipt），M-D2-8 补 `erase_transport_body` 必需参数（purge_revision + now），M-D2-10 锚点同步严格解析重构，M-D2-11 整块替换 gate report 消费块，新增 syntax-invalid vs behavioral 输出区分（syntax 不计入 KILLED）。**Round-6 Ready 前返修已落地（2026-08-31）**：(1) NO_REPEAT 完整证据——所有路径（含 NO_REPEAT）统一经 `_toctou_reverify_pass_b` 完整 reverify（**禁止**只读 checkpoint.state 后直接 continue）；唯一例外 archive cp ∈ {pending,erasing} + LIVE=acked 仅豁免 checkpoint.state 的合法单向变化，其余 operation 8 字段 + checkpoint 5 字段任何 drift 均 fail closed；LIVE acked → ack_digest 必须 lowercase 64-hex；`toctou_drift` 真实递增（不恒 0）。(2) External verify-only 真正落地——删除主流程手写 for+break，实际调用统一 `_bind_archive_external_ref` binder（按 `validated.conversation_id` + `owner_key` 恰好一条；0/≥2/错 conversation/owner → 具名 `EXTERNAL_VERIFICATION_FAILED` 携带 binder reason）；严格解析 id/conversation_id/owner_key/receipt_digest；receipt lowercase 64-hex；receipt+state 匹配后复用 `build_scan_providers` external.payload.v1 scan 要求 residual total==0；禁止 adapter / 任意 LIVE row fallback；新增 EXTERNAL_VERIFIED 真实成功正例。(3) archive facts 类型闭合——`_require_field`/`_require_strict_int`/`_require_str`/`_require_canonical_uuid`/`_require_64hex_lower`/`_optional_64hex_lower` 明确 helper 族，禁止 str()/int() 隐式转换，缺失/类型不符分别具名 `ARCHIVE_FACTS_FIELD_MISSING`/`ARCHIVE_FACTS_TYPE_INVALID`，所有解析异常归一化为 RestoreReplayError 不泄漏 ValueError。(4) 判别测试重写——TOCTOU 测试改为 pass B tx 内经 monkey-patch maintenance-lock wrapper 同 tx 注入 drift（pass A 完成后、reverify 前），断言 TOCTOU_DRIFT_FIELDS + toctou_drift=1 + pass_a_drift=0 + rollback，覆盖 2 operation 字段 + 2 checkpoint 字段 + NO_REPEAT 例外下 drift；duplicate external 用 operation 真实 conversation_id 断言精确 `EXTERNAL_ARCHIVE_DUPLICATE`；新增 missing/wrong binding/receipt mismatch/final-scan residual/唯一记录+scan zero 成功五类真实 PG 测试；双 owner rollback 强化——spy 证明 owner A（按 checkpoint stable_identity=id::text 排序第一）先真实执行 + 新 session 完整前后快照比对 checkpoint/operation fence/正文/源表。(5) mutation——新增 M-D2-18（NO_REPEAT 绕过完整 reverify）/ M-D2-19（duplicate binder bypass）/ M-D2-20（external final-scan bypass）；M-D2-17 改绑 receipt_mismatch（不再在不存在 final scan 时称 final-scan kill）；`apply_mutation` 加 `compile()` 预校验（syntax-invalid 强制重做）。**当前验证基线（Round-6）**：test_s6i3_d_restore_replay.py **77 tests collected 全 pass**；composition 全量 **961 passed**（6 skipped）；mutation kill **15/15 behavioral KILLED**（syntax-invalid=0；每条 byte-level 修改 + `compile()` 预校验 + 绑定真实 invariant test + mutation 存在 exit=1 + 恢复后 exit=0 + SHA-256 byte-identical）；ruff clean / mypy baseline 243 historical / 0 回归 / git diff --check clean / engineering-docs `--full` 全绿（td-032 已按 Round-6 实际行数 1655 / 1960 更新登记）。**Round-7 有界重构返修已落地（2026-09-01，实现 HEAD `0311bbfa`，base `74ae0d53`；PR #602 OPEN/Draft，分支 `feature/req041-047-r1-s6-i3-d-d2-restore-replay`，writer `restore_replay_executor` 已 registered=FENCE_M）**：(1) 接住四个 participant 真实 outcome——`_ParticipantOutcome` Protocol（只读 property 兼容 frozen dataclass）+ `_classify_participant_outcome` 结构化分类（blocked=True 必须带非空 block_reason + ack None + erased False → `BLOCKED_KEPT` + `owners_blocked_kept` 计数 + 稳定 reason，restore-before-open gate 消费保持关闭；blocked=False 必须 erased + lowercase 64-hex ack → `LOCAL_CLEARED`；非法 shape → `PARTICIPANT_OUTCOME_INVALID` fail closed + rollback）；新增真实 PG blocked 正例（active legal hold → 不误记 cleared + gate 关闭）。(2) 进入 pass B 写入前一次性预验证全部 owner_key ∈ LOCAL ∪ NON_LOCAL——unknown owner 在进入写事务前 fail closed（双 owner「local 先写 + unknown 后到」测试 spy exclusive lock 证明零 partial commit、与排序无关）；pass B 防御分支保留 raise+rollback。(3) 删除 NO_REPEAT 双 checkpoint-state 读取——`_toctou_reverify_pass_b` 在同一 cp_row 快照返回 terminal evidence，最终 action 只消费该结果（删除 `_read_checkpoint_state_live`）；判别测试 patch `_load_checkpoint_row` 让 reverify 同快照见 acked 而真实 DB erasing（旧双读实现真红）。(4) `_require_canonical_uuid` 严格化——raw 必须本就是 str 且 `str(uuid.UUID(raw))==raw`；UUID 对象/大写/无连字符/非法字符串/int/bytes 全部具名 `ARCHIVE_FACTS_TYPE_INVALID`。(5) mutation harness 重写为结构化 pytest/JUnit 分类（`--junitxml` 解析 `<failure>`/`<error>`；仅实际执行后的 invariant failure（断言签名 AssertionError/assert/Failed）计 KILLED；import/collection/usage/internal(exit 2/3/4)/setup/fixture(`<error>`)/call 阶段非断言 crash/timeout/no-tests(exit 5/0 收集)/survived(exit 0) 一律不计；`compile()` 预检 syntax-invalid 单独分类）；新增分类器自测 `test_s6i3_d_mutation_classifier.py`（15 项）；分母按实际 21 项运行时计算（不预写数字）；修复 4 项 Round-7 后 stale anchor（M-D2-8/11/18/20）；新增 6 项 mutation（M-D2-21 blocked 误记 cleared / M-D2-22 预验证 bypass / M-D2-23 canonical UUID str() 强转 / M-D2-24 scan total 默认 0 / M-D2-25 malformed candidate 跳过 / M-D2-26 reverify 永不 terminal）。(6) 低成本闭合——malformed external candidate → `ARCHIVE_FACTS_TYPE_INVALID`；external scan total 缺/类型错误/负数 fail closed（不默认 0）。**当前验证基线（Round-7）**：test_s6i3_d_restore_replay.py **88 全 pass** + 分类器自测 **15 全 pass** = 103；composition 全量 **987 passed**（6 skipped）；mutation kill **21/21 结构化 invariant-failure KILLED**（byte-identical restore）；ruff clean / mypy baseline 243 historical / 0 回归 / git diff --check clean / engineering-docs `--full` 全绿（td-032 已按 Round-7 实际行数 1812 / 2351 更新登记）。**严格停止保持**：不 Ready、不评分、不合并、不 closeout；D1b production wiring / D2 production wiring / PR-D / PR-E / C1 / S5 production wiring / capability flip / 六 erase 入口生产可达仍未启动；仅 metaedu_test。
  3. **D1a + D1b 是否合并为单 PR D1**——**已冻结**为三个独立 PR（D1a / D1b / D2 各自独立 PR，不合并）
  4. **F-matrix 7/12 + F10 M6 test contract 增强 PR**（独立测试 contract 增强）；**TD-104**（PR-A schema/test alignment 残项）；**PR-E**（release drill 五阶段 canary，本地无法执行——按 R1-AC12 字面降级为 contract-tested 验证 + 登记生产门禁）
  5. **C1 Durable Core 总验收** / **S5 production wiring** / **registry capability 翻转** / **六 erase 入口生产可达**——本轮**不启动**

验证状态：**D1a 42 项真实 PG 测试 + 15 项纯内存 decoder 负例 + mutation kill 20/20 KILLED + composition 828 passed**；PR #598 squash merge 入 main（mergeCommit `5868831e`）——Draft 三路 required checks 全绿（run 33096654991）+ Ready required checks 全绿（run 33097987039，Backend full / Backend iteration / Frontend / Engineering docs 全 SUCCESS）+ `scripts/check-review-score-submit --base 8a836733 --pr 598` PASSED（PR #555 / commit `2b801a60` 落地门禁） + 评分 97 Original（7 维：15/20/20/15/15/10/5）。**严格停止条件**（本轮禁止越过）：需要新 schema/migration/enum/CHECK；需要修改 S5 状态机/锁序/写者矩阵；需要原始敏感 ref 才能重放（external/runtime ref_value / runtime_session_ref）；需要调用 external/runtime adapter；缺少独立 archive sink 或维护互斥机制；owner 六元组或 receipt 无法证明；需要 production wiring/capability flip；冻结契约与当前可执行能力冲突；或需越过 specs §10.5「人工签字后按 tenant/canary 开启 scheduler」/§S6-7.1「V1 不支持 purge 开启时仍有旧 Writer 进程在线」——立即停在 Draft 并报告方案，不自行架构裁决。**禁止修改**：Score Log、Metrics、migration 043、schema、registry capability、门禁脚本、KNOWN_ISSUES、CI 配置或阈值；F10/TD-105 已合并内容；C1 / PR-E / S5 production wiring / capability flip 不在本任务范围启动。**数据库硬边界**：禁止 drop / truncate / reseed / 重建开发库 `metaedu`；后续测试只能使用 `metaedu_test`；任何 destructive DB 命令前必须先打印并确认实际 database name，若不是 `metaedu_test` 立即停止。
**D1a NOT-RED 历史登记（已在本轮修复）**：第一轮 4 项 NOT-RED（M2 ref_value leak / M4 count bypass / M7 sort bypass / M9 cross-tenant bypass）→ 全部转 KILLED；M7 mutation 由「移除 sorted」改为「反转 records」（producer 反向输出 corruption），因 SELECT 已 ORDER BY id，原 mutation 不可观察，PR body / current-work 不再称其证明 sorted() 调用不可删除。

交接备注：本任务承接 TASK-R1-S6-I3 后续接力序列的 (1) PR-D 入口；从冻结契约 §S6-8/12/13/14 + §S6-15.5 TD-106 收口与当前 main 实现能力出发重新推导，不复制旧 scaffold；S6I2_PENDING_WRITERS `restore_replay_executor` 已转 `registered`=FENCE_M（historical：早期「待本任务完成后转 registered」已被 D2 落地 supersede）；M 类维护路径（replay executor）与 retention/audit jobs 互斥（冻结声明）；external/runtime 未 ACK 项经 replay 保持 `blocked` + reconcile（**不调用 adapter、不冒充已 erase**）。

## 下一批候选任务

按"建议执行顺序"排序；候选区只保留近期 1 到 3 个入口，完整任务池回 `docs/01-product-planning/04-backlog.md` / `docs/03-engineering-governance/technical-debt.md`。

| 优先级 | 任务 | 状态 | 建议下一步 | 事实源 |
|--------|------|------|------------|--------|
| P1 | R1-S6 PR-E release drill 五阶段 canary（真实 PG / 备份 / 流量开关） | ⚫ 待办，独立后续 PR | 本地无法执行（无生产基础设施 + 无备份保留 runbook 执行环境）——按 R1-AC12 字面降级为 contract-tested 验证；登记生产门禁 | [Plan §S6-7](../02-delivery-plans/02-plans/2026-07-27-req-041-047-r1-retention-purge-recovery-plan.md) |
| P1 | R1-S6 F-matrix 7/12 + F10 M6 test contract 增强 | ⚫ 待办，独立后续 PR | F-matrix 7/12 mutation NOT-RED（M-F2/F3/F4/F5/F6/F8/F12）+ F10 M6（completed 绕过最终扫描）判别载体补齐；M6 真实 PG 路径需 G1/G2/G3 cleared + 所有 owner acked + final scan 非零 → blocked (scan reason) 的测试矩阵 | [TD-105](technical-debt.md#td-105-r1-s6-f10-实现承接契约纠偏后解除-skip--真实-pg-判别) closeout note / F-matrix #590 mutation follow-up |

## 最近完成

最近完成区默认最多保留 20 行（按 `docs/03-engineering-governance/01-rules/workbench.md#保留策略` 强约束）。超过 20 行时，必须一次性批量归档，只保留最新 12 行；第 13 行及以后全部移出。不得每次只移动 1 行。本表只承担"近期完成窗口"的角色，详细验证、行为变化、PR 描述和复盘见 `docs/03-engineering-governance/work-log.md`、对应技术债总账、plan 或 PR。

按"最近优先"（最新任务在最上）排序：

| 日期 | 任务 | 状态 | 摘要 | 事实源 |
|------|------|------|------|------|
| 2026-08-28 | R1-S6-I3-D D1b 专用 MinIO ledger archive sink + 不可变 commit-graph 发布协议（两阶段 API 拆分；三面 P1 修复闭环 + squash merge 入 main） | 🟢 完成（D1b 子阶段；TASK-R1-S6-I3-D 仍 🟡 进行中） | PR #600 squash mergeCommit `01c84f7c`；评分 94 Original；47/47 composition + 6/6 opt-in real MinIO + 11/11 mutation；D1b wiring / D2 / PR-D / PR-E / C1 / S5 wiring / capability flip / 六 erase 未启动 | [PR #600](https://github.com/MarkDanile/MetaEduBase/pull/600)（mergeCommit `01c84f7c`）/ [work-log](work-log.md) / [score 94](04-retrospectives/review-score-log.md) / [fact-audit](04-retrospectives/r1-s6-i3-d-fact-audit.md) |
| 2026-08-28 | R1-S6-I3-D D1a bounded read-only ledger snapshot codec（三轮 P1 治理闭环 + squash merge 入 main） | 🟢 完成（D1a 子阶段；TASK-R1-S6-I3-D 仍 🟡 进行中） | PR #598 squash mergeCommit `5868831e`；评分 97 Original；57/57 D1a + 828 composition + 20/20 mutation；D1a 已并入 main，D1b/D2/PR-D 未启动 | [PR #598](https://github.com/MarkDanile/MetaEduBase/pull/598)（mergeCommit `5868831e`）/ [work-log](work-log.md) / [score 97](04-retrospectives/review-score-log.md) / [fact-audit](04-retrospectives/r1-s6-i3-d-fact-audit.md) |
| 2026-08-26 | R1-S6-I3-F10 settlement T1/T2 hold 推进故障矩阵真实 PG 判别 + TD-105 实现承接 | 🟢 完成 | PR #596（squash `c0ec008d`）；评分 97 Original；8 项 PG 测试 + 7/8 mutation 真红（M6 NOT-RED）；净 diff 3 文件 1005+/23- 无生产代码；**TD-105 已完成并关闭**；F10 不直接 completed；PR-D/E/C1/S5 wiring 未启动 | [PR #596](https://github.com/MarkDanile/MetaEduBase/pull/596)（squash `c0ec008d`）/ [work-log](work-log.md) / [score 97](04-retrospectives/review-score-log.md) |
| 2026-08-26 | R1-S6-I3 root bounded integration: S6-F1..F14 故障矩阵 + TD-106 方案 A + scope 收敛 + main closeout | 🟢 完成 | root PR #586（squash `68fafd81`）+ main closeout；评分 94 Original；scope = F1-F14 故障矩阵 + TD-106 方案 A bounded integration；PR-D/E scaffold 已撤回；**TD-106 已完成并关闭**；F10 仍 skip（TD-105 承接）+ PR-D/PR-E/C1/S5 wiring/capability flip 均未启动 | [PR #586](https://github.com/MarkDanile/MetaEduBase/pull/586)（squash `68fafd81`）/ [work-log](work-log.md) / [score 94](04-retrospectives/review-score-log.md) |
| 2026-08-25 | R1-S6-I3-F10 S6-F10 契约核对纠偏 + TD-106 决策门禁（contract-first，纯文档） | 🟢 完成 | PR #591（squash `738be6f9`）；评分 89（Original）；§S6-15 冻结入 main；**TD-106 实现已随 #586 入 main 完成**（详细见 work-log + [TD-106](technical-debt.md#td-106-r1-s6-settlement-success-不写-ledgerbinding实现-vs-s5-c-1-冻结契约缺口pre-existing)） | [PR #591](https://github.com/MarkDanile/MetaEduBase/pull/591)（squash `738be6f9`）/ [work-log](work-log.md) / [score 89](04-retrospectives/review-score-log.md) |
| 2026-08-24 | R1-S6-I3-A schema/test alignment bounded repair（squash 入 #586） | 🟢 完成 | squash 入 #586；评分 92（Original）；幽灵列对齐真实 schema + acked fixture 合法化 + 独立 CHECK 负例 + F3 补种；fresh PG 043 24/24 + composition 750；**#586 已合 main**；TD-104 保持承接 | [PR #589](https://github.com/MarkDanile/MetaEduBase/pull/589)（squash `f6062466`）/ [work-log](work-log.md) / [score 92](04-retrospectives/review-score-log.md) |
| 2026-08-24 | R1-S6-I3-B restore replay 持久状态域契约纠偏（contract-first，纯文档） | 🟢 完成 | PR #587（squash `66674f23`）；评分 85；三层 CHECK 闭集 + replay 路由表 + 判定方式冻结；**#586 已合 main**；TD-104 + REQ-047 | [PR #587](https://github.com/MarkDanile/MetaEduBase/pull/587)（squash `66674f23`）/ [work-log](work-log.md) / [score 85](04-retrospectives/review-score-log.md) |
| 2026-08-20 | R1-S6-I2 Writer conformance suite + body/ref orphan inspection | 🟢 完成 | PR #584（merge `ad7ac3e5`）；评分 88；3 writer spec + 六类 verify 巡检 + Run 行锁；21 项专项 + 726 composition；TD-100~103 + REQ-047；S6-I3/C1/S5 wiring 未启动 | [PR #584](https://github.com/MarkDanile/MetaEduBase/pull/584)（merge `ad7ac3e5`）/ [work-log](work-log.md) / [score 88](04-retrospectives/review-score-log.md) |
| 2026-08-19 | R1-S6-I1 Retention workers（run_event_retention + run_audit_retention + migration 043） | 🟢 完成 | PR #582（squash `f5072ec6`）；评分 87（基线 `d1427567`）；两 worker + 043 guard + 两处 S5 修复落地；三面返修+决 A 测试兼容升级后 P0/P1=0；Backend 2649/1/4/0 + mutation 18/18 + 043 往返稳定；S6-I2/I3/C1/S5 wiring 未启动；TD-097/098/099 + REQ-047 | [PR #582](https://github.com/MarkDanile/MetaEduBase/pull/582)（squash `f5072ec6`）/ [work-log](work-log.md) / [score 87](04-retrospectives/review-score-log.md) |
| 2026-08-19 | R1-S5 Root Integration: settlement idempotency key 对齐 + B/C/D 联合组合根 | 🟢 完成 | root PR #577（squash `636fc425`）合并；评分 92（基线 `995aa223`）；126 专项 + composition 665 + mutation 8 组 + Backend 2600/1/4 + Frontend 326+55；production erase 入口仍不可达；follow-up REQ-047 + TD-093/095/096 + td-032 | [PR #577](https://github.com/MarkDanile/MetaEduBase/pull/577)（squash `636fc425`）/ [work-log](work-log.md) / [score 92](04-retrospectives/review-score-log.md) |
| 2026-08-19 | R1-S5 SCH-D: Settlement & Retry-Reconcile（squash 入 root） | 🟢 完成 | 已 squash 入 root PR #577（`5033efc5`）并随 root 合并（`636fc425`）；child 正式评分 92（Original）；23 专项 + 12/12 mutation kill + composition 646 passed | [PR #579](https://github.com/MarkDanile/MetaEduBase/pull/579) / [score 92](04-retrospectives/review-score-log.md) |
| 2026-08-19 | R1-S5 SCH-C: Rebuild & Seeding（squash 入 root） | 🟢 完成 | 已 squash 入 root PR #577（`a8f4d561`）并随 root 合并（`636fc425`）；child 正式评分 92（Original）；40 专项 + 27/27 mutation kill + composition 623 passed | [PR #578](https://github.com/MarkDanile/MetaEduBase/pull/578) / [score 92](04-retrospectives/review-score-log.md) |
| 2026-08-17 | R1-S5 SCH-A Claim & Lease 实现（migration 042 + ConversationPurgeScheduler） | 🟢 完成 | migration 042 + claim/lease 服务（四转移 expected-epoch CAS、tenant 上限 4、takeover 后强制聚合）；评分 93（最终 P0/P1/P2=0）；29 专项 + 13/13 mutation kill + Backend 2504/1/4；**SCH-A 完成不代表 Scheduler 已启用**；follow-up REQ-047 | [PR #575](https://github.com/MarkDanile/MetaEduBase/pull/575)（squash merge `36d091a4`）/ [work-log](work-log.md) / [score 93](04-retrospectives/review-score-log.md) |
| 2026-08-17 | R1-S5-D-A SCH-A Lease Carrier 契约纠偏（contract-first，纯文档） | 🟢 完成 | durable lease carrier 契约已纠偏：`updated_at` 退出租约事实源、migration 042 冻结（SCH-A 落地）、三态×四转移 epoch CAS 全函数、owner entry 门禁、SCH-9..16（反例矩阵 53→61 冻结验收载体）；评分 90（最终 P0/P1=0）；**纠偏完成不代表 migration 042 或 SCH-A 已实现**；follow-up REQ-047 | [PR #573](https://github.com/MarkDanile/MetaEduBase/pull/573)（squash merge `3438c53b`）/ [work-log](work-log.md) / [score 90](04-retrospectives/review-score-log.md) |
| 2026-08-16 | R1-S5-D Scheduler 契约冻结（contract-first，纯文档） | 🟢 完成 | S5-SCH-0..5 全卷（状态机/锁序/写者矩阵/四 slice 拆分/53 项反例映射/REQ-047 分流）；评分 87（最终 P0/P1=0）；**契约完成不代表 Scheduler 实现完成**；SCH-A/B/C/D 未开工，B/C/D 联合 merged-boundary | [PR #571](https://github.com/MarkDanile/MetaEduBase/pull/571)（squash merge `253e53e4`）/ [work-log](work-log.md) / [score 87](04-retrospectives/review-score-log.md) |
