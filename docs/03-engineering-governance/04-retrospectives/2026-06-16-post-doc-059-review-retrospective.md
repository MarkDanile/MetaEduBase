# 2026-06-16: DOC-059 后近期任务评审复盘

Trigger:
- 用户要求“按流程评审最近的工作完成事项”，并指定上一次评审基线为 `DOC-059`。
- 评审后继续要求“按流程再复盘下”。

Scope:
- 覆盖 `review-score-log.md` 中 2026-06-16 新增的 11 条 Codex 复评记录。
- 复盘粒度按交付主题聚合，不逐个微 PR 重复分析。
- 本轮复盘不新增规则；已发现的真实业务验收缺口统一分流到 `REQ-014`。

## Signals

| 信号 | 证据 | 影响 |
|------|------|------|
| 质量均值小幅抬升 | 评分记录从 40 条增至 51 条；平均分 84.8 → 85.2；本轮新增 11 条平均分约 86.4。 | 规则、脚本门禁和交叉评审仍在稳定提高交付下限。 |
| 返工信号没有下降 | 一次关闭率 60% → 57%；返工率 40% → 43%。 | 近期任务更接近真实 RAG / 数据质量问题，发现 follow-up 是必要代价，但不能长期依赖补丁链。 |
| 流程扣分率仍偏高 | 流程扣分率 63% → 65%；本轮多条记录涉及状态漂移、真实 PG 样例缺口或合并后回填。 | 最大风险仍不是“写不出来”，而是多事实源、真实验证和关闭口径容易不同步。 |
| 优秀任务共同点清晰 | `TD-054` 93 分，`BUG-005` 91 分：都有真 PG 前后对比、明确指标和回归测试。 | 高质量交付的样板是“真实数据 + 自动化测试 + 可量化前后差异”。 |
| AI Chat / RAG 仍是主风险区 | `BUG-003 + REQ-012`、`REQ-013`、`BUG-007` 都指向同一问题：机制存在，但真实样例和最终回答质量未闭环。 | P2 的核心不应继续堆新通道，而应先把真实 grounding 验收跑通。 |
| 规则没有继续膨胀的必要 | 本轮问题已能被现有评分卡、工作台门禁和跨事实源一致性门禁识别。 | 当前优先级是执行和真实样例补齐，而不是继续增加长规则。 |

## Root Causes

| 根因 | 类型 | 说明 |
|------|------|------|
| RAG 质量从“链路存在”进入“真实可用”阶段 | 需求塑形不足 | `REQ-012` / `REQ-013` 已补多路 evidence 与 Context Packer，但“python 的基本数据类型有哪些？”这类真实问题仍需要样例数据、重建动作和最终回答验收。 |
| Mock 测试证明机制，不能证明真实数据质量 | 实现 / 验证问题 | `REQ-013` 17 条 mock tests 有价值，但无法覆盖真实 PG、历史 chunk、section path、metadata 和 prompt 组装质量。 |
| 数据链条是耦合系统 | 架构 / 数据问题 | parse、chunk、section metadata、embedding、tsvector、KG 回源、sources UI 任一环节漂移，最终都会表现为“AI 只拿到目录或孤立证据”。 |
| 多切片收口后事实源容易滞后 | 流程问题 | `BUG-006`、`BUG-007`、`TD-051` 均出现过交付记录或总账状态滞后，本次评审已修正局部漂移。 |
| Follow-up 链条有效但成本上升 | 工具习惯 | 小 PR 串行降低了单次风险，但如果没有阶段性真实验收，会在后期形成多条“已完成但仍待综合复测”的尾巴。 |

## Corrective Actions

| ID | 类型 | 状态 | 动作 | 验证 |
|----|------|------|------|------|
| REQ-014 | REQ / RAG / 数据回填 | 🟡 Shaping | 统一承接 `REQ-013`、`BUG-007`、`BUG-006` 的真实 PG 样例、数据重建和最终回答 grounding 验收。 | 已新增 Requirement、Backlog、Milestone、Iteration 和 current-work 候选入口。 |
| REQ-013 状态同步 | 事实源修正 | 🟢 完成 | 将 W25 迭代页中 `REQ-013` 从 `Ready` 修正为 `Done`，并声明真实 PG 验收转入 `REQ-014`。 | `scripts/check-engineering-docs` 已通过。 |
| BUG-007 交付记录修正 | 事实源修正 | 🟢 完成 | 将过期的“实际修复留维护者下个 PR”改为 PR #303 已合并，并把 reparse/backfill 交给 `REQ-014`。 | `scripts/check-engineering-docs` 已通过。 |
| TD-056 状态修正 | 事实源修正 | 🟢 完成 | 将总账状态从 `🔵 就绪` 修正为 `🟢 完成`，剩余返回值契约已由 TD-057~TD-066 收口。 | `scripts/check-engineering-docs` 已通过。 |

## Follow-up Check

| 日期 | 检查项 | 结果 |
|------|--------|------|
| REQ-014 完成后 | “python 的基本数据类型有哪些？”必须命中正文 evidence，不得只以目录为主证据。 | 待验证。 |
| REQ-014 完成后 | 记录真实 PG 样例的 parse / chunk / embed / tsvector / KG 回源或等价重建命令、环境和退出结果。 | 待验证。 |
| REQ-014 完成后 | AI Chat 回答、`[N]` 引用、文档级来源列表和打开行为形成一次端到端验收。 | 待验证。 |
| 下一轮 10 条评审后 | 一次关闭率回到 60% 以上，流程扣分率向 55% 以下移动。 | 待观察。 |
| 下一轮阶段复盘 | 若最近 5 条评分平均分 > 92，按高分质量校准抽查是否偏宽。 | 待观察。 |

## Conclusion

本轮结论比较清楚：Harness 规则体系本身没有失效，甚至已经能稳定暴露状态漂移和验证缺口；真正需要收口的是 P2 RAG 的真实业务样例闭环。下一步不宜继续增加规则，而应让 `REQ-014` 把“机制正确”推进到“真实回答可用”。
